# Visual assets

- `archive/` contains inactive inspiration and superseded project graphics.
- `qa/` contains screenshots used by audits and responsive checks.
- Runtime website graphics remain in `website/public/images/` because Vite serves
  that directory directly.

New inspiration or source graphics belong in `archive/`. New automated captures
belong in the appropriate `qa/` subfolder; do not add screenshots to the project
root.
