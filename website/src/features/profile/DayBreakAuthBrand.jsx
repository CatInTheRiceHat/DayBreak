import './public-auth.css';

export function DayBreakAuthBrand() {
  return (
    <div className="db-auth-brand" aria-label="DayBreak">
      <span className="db-auth-brand__mark" aria-hidden="true">
        <span />
      </span>
      <span className="db-auth-brand__copy">
        <strong>DayBreak</strong>
        <small>A brighter way to scroll.</small>
      </span>
    </div>
  );
}
