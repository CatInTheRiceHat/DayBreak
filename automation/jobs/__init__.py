"""Recurring-job orchestration namespace.

Executable jobs will be introduced here only after their current entry points
have characterization coverage. Reusable business logic belongs outside this
package, primarily in ``core`` and ``integrations``.
"""
