VITE_API_URL=http://localhost:8000

# Supabase (auth + profiles + avatar storage).
# The URL is your project's API URL; the anon key is the PUBLIC publishable key
# (safe to ship in the frontend — Row Level Security protects the data).
# Leave these blank to run the app without auth: the profile/sign-in screens show
# a calm "sign-in is being set up" state and the rest of Chrysalis works as normal.
VITE_SUPABASE_URL=https://vtpohhapzgfhgjtwthwe.supabase.co
VITE_SUPABASE_ANON_KEY=
